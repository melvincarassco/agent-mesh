# Unit Tests (`tests/unit/`)

Contains fast, isolated tests for individual functions, utilities, schema parsing, and business logic methods.

## Rules

- No network requests allowed in unit tests.
- Execution speed target: <10ms per test file.
