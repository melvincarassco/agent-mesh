# Terraform Infrastructure Root (`infrastructure/terraform/`)

Terraform 1.7+ infrastructure configuration files for Carassco Labs GCP foundation resources.

## Terraform Commands

```bash
# Initialize Terraform provider & remote state
terraform init

# Validate configuration syntax
terraform validate

# Review execution plan
terraform plan

# Apply infrastructure changes (CI Pipeline only!)
terraform apply
```
